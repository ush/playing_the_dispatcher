*для этой штуки нужно название получше*

**work in progress. nothing works yet**

Как запустить?

```
git clone https://github.com/kubernetes/kubernetes $KUBE_DIR
cp -r * $KUBE_DIR
cd $KUBE_DIR
go run pkg/mytest/main.go
```
